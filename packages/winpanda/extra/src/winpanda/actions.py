"""Winpanda: Action definitions.
"""
import sys


def setup(install, repository):
    """Setup DC/OS from scratch.
    """
    print(f'{sys.argv[0]}: Got the "setup" command!')
    # _do_bootstrap(install, repository)  # TODO: Pending implementation.
